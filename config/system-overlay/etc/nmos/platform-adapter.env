# Optional NM-OS platform adapter overrides.
# Keep defaults unless you are porting NM-OS to a non-Debian platform.
#
# NMOS_TOR_USER=debian-tor
# NMOS_GDM_USER=Debian-gdm
# NMOS_RUNTIME_DIR=/run/nmos
# NMOS_STATE_DIR=/var/lib/nmos
