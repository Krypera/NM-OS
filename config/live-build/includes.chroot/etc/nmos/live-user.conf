# NM-OS live-session defaults used by the pre-login greeter handoff.
# LIVE_PASSWORD is optional. When unset, NM-OS generates a random password
# per boot and stores it in /run/nmos/live-user-password for Debian-gdm.
LIVE_USERNAME="nmos"
